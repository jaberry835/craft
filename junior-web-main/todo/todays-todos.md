I'd like to build mcp tool calls , the first one will be markitdown, that create a mark down version of word/excel files loaded in to the workspace

I'd like to build a publish feature, take the data I have and instructinos and I will have an agent that builds a publish profile based on an existing react application and publishes a static version to azure storage some where.... and have the link for it up in the right hand corner... 


I'd like to build an automation layer --- my workflow for demo will be, I pull in files as a researcher on a topic driven by the chat interface that will go search data sources, it brings those sources together by pulling files or bit of information of interest in to markdown files.   It will be like a dosseir on a person, and recent communications and travel.   

I then would like to have automation function that asks those systems that it got information from for new information, update it's information... this will then add new information on that person, update the blob stor files, and add to the publish, and call out new (since XYZ) 

Basically I imagine an automation workflow somewhere here... looking for best ways of doing this , as I think we will need to spin off compute to do this. not sure how

