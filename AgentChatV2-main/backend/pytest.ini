[pytest]
asyncio_mode = auto
testpaths = tests
markers =
	smoke: fast smoke tests that validate core API behavior without external Azure dependencies
	live: live end-to-end smoke tests against a running backend and real integrations
