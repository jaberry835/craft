# Core dependencies - Agent Framework
# Uses agent-framework-a2a for A2A protocol support including:
# - A2AAgent: Wraps remote A2A endpoints
# - A2ACardResolver: Agent card discovery
# - ClientCallInterceptor: Authentication for secured A2A endpoints
# - Streaming: Real-time updates via SSE
# Uses agent-framework-ag-ui for AG-UI protocol support (standardized SSE events)
agent-framework-azure-ai==1.0.0b260114
agent-framework-a2a>=1.0.0b260219
agent-framework-ag-ui>=1.0.0b260219
agent-framework-core>=1.0.0rc1

# A2A Protocol SDK (a2a-sdk is the correct package - NOT 'a2a' which is a web scraping tool)
# Provides: a2a.client.A2ACardResolver, a2a.client.middleware.ClientCallInterceptor, a2a.types.AgentCard
a2a-sdk>=0.3.5

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
sse-starlette>=1.8.0
# Pin watchfiles and websockets to versions with prebuilt wheels
watchfiles>=1.0.0
websockets>=13.0

# Azure SDKs (use DefaultAzureCredential for managed identity)
azure-identity>=1.15.0
azure-cosmos>=4.5.0
azure-storage-blob>=12.19.0
azure-search-documents>=11.4.0
azure-ai-textanalytics>=5.3.0

# Authentication
msal>=1.26.0
PyJWT>=2.8.0
cryptography>=41.0.0  # PFX certificate parsing for OBO token exchange

# Observability
azure-monitor-opentelemetry>=1.2.0
opentelemetry-api>=1.22.0
opentelemetry-sdk>=1.22.0
opentelemetry-instrumentation-fastapi>=0.43b0
# Pin: 0.4.14 removed SpanAttributes.LLM_SYSTEM which agent-framework-core uses
opentelemetry-semantic-conventions-ai>=0.4.13

# Document parsing
openpyxl>=3.1.0
pymupdf>=1.24.0
python-docx>=1.1.0
python-pptx>=0.6.23

# Azure Document Intelligence (rich layout-aware extraction for PDFs, scans, Office)
azure-ai-documentintelligence>=1.0.0

# Image processing (resize/compress images for vision API storage in Cosmos DB)
Pillow>=10.0.0

# Utilities
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
httpx>=0.26.0
tiktoken>=0.5.2

# MCP client - pin to stable version
mcp>=1.13.0
