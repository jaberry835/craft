# Cloud Scan Inputs

Place immutable Azure scan exports here. Prefer structured JSON that records collection time, collection method, and scope.

Redact secrets before import. A scan is evidence of observed state at its collection time, not proof of continuous compliance.