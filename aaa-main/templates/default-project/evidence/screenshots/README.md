# Screenshot Evidence

Browser captures and root-level image uploads default to this directory.

AAA browser captures include an adjacent JSON file with the source URL, capture time, browser mode, viewport, and capture dimensions.
